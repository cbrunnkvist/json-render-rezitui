# 13 - Starship Bridge

**Use case**: Starship control panel with AI co-pilot.

**Demonstrates**: Host UI + AI slot, dangerous action confirmation via `ui.dialog()`, ID prefixing in AI prompts.

**To run**: `npm start` after setting `.env`. The AI generates a control panel. Click "Eject Warp Core" to see the confirmation dialog.
